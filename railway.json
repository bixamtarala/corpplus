{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "sh -c 'uvicorn phase2_backend.main_phase2:app --host 0.0.0.0 --port ${PORT:-8000}'",
    "restartPolicyMaxRetries": 5,
    "restartPolicyWindowMs": 60000
  }
}
